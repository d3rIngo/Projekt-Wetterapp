{
  "compilerOptions": {
    // Konfiguration der TypeScript-Compileroptionen
    "paths": {
      // Definieren von Alias-Pfaden für Module-Importe
      "@/*": ["./src/*"] // Alias-Pfad für Importe von Modulen im "src"-Verzeichnis
    }
  }
}
